app.name=AccordionListExample

app.framework=touch

app.classpath=${app.dir}/app.js,${app.dir}/../src/ux,${app.dir}/app

# this property specifies a comma separated list of paths containing
# resources to copy to the build directory
app.resource.paths=

app.build.dir=${workspace.build.dir}/${app.name}

app.framework.version=2.4.2.571

app.cmd.version=5.1.3.61
